__pycache__/
*.pyc
node_modules/
.DS_Store

# Lighthouse HTML reports — regenerated by `npm run lighthouse`
audit/reports/

# NOTE: assets/js/bundle.min.js is NOT ignored. It is a build artifact, but it
# is also what the pages actually load, so it has to ship with the site.

# Playwright
test-results/
playwright-report/

# Product and keyword research. Kept local: the repo is public and Pages
# publishes everything in it.
research/
